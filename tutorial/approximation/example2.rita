# Iris Script file to practice data approximation
# We use Lagrange interpolation with 3 points
# rita creates a vector (or field) which is the interpolated vector
#
approximation name=Tab1 file=ex2.dat piecewise_lagrange
print f
print Tab1
exit
